#include <stdio.h>
#include <stdbool.h> 

int main(void)
{
    int value = 50;

    // Range [min,max]
    int min;
    int max;
    
    // TODO 
    
    printf("%d <= %d <= %d\n", min, value, max);

    return 0;
}
